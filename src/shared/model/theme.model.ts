/**
 * Represents the user's theme preference.
 * - 'light': Force light theme
 * - 'dark': Force dark theme
 * - 'system': Follow the OS/browser preference via prefers-color-scheme
 */
export type ThemePreference = 'light' | 'dark' | 'system';
